import React, { Component } from "react";
import AppBar from "../common/AppBar";

class Connect extends Component {
  render() {
    return (
      <div>
        <AppBar />
        <div> This is the Connect component </div>
      </div>
    );
  }
}

export default Connect;
